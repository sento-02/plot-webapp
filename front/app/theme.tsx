import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // この色を変更して任意の色を設定します
    },
    secondary: {
      main: '#dc004e', // この色を変更して任意の色を設定します
    },
  },
});

export default theme;
